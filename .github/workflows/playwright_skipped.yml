name: Playwright UI E2E
on:
  pull_request:
    paths-ignore:
        - "performance_tests/**"
        - "ui_tests/**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No build required"'
