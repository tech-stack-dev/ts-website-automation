name: Playwright UI E2E

on:
  push:
    branches: [ "main" ]
    paths-ignore:
        - "ui_tests_playwright/**"
  pull_request:
    branches: [ "main" ]
    paths-ignore:
        - "ui_tests_playwright/**"

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - run: 'echo "No build required"'
